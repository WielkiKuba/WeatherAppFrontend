services:
- type: web
  name: nextjs-static
  runtime: node
  plan: hobby
  buildCommand: yarn; yarn build
  startCommand: yarn start
  autoDeploy: false
  envVars:
  - key: NODE_ENV
    value: production
